const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres.bnrvteghbgefbraizebh:CaXRg4vEScCh7U6W@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres', ssl: { rejectUnauthorized: false, checkServerIdentity: () => undefined } }); pool.query('SELECT NOW()').then(r => { console.log('Connected!', r.rows[0]); pool.end(); }).catch(e => { console.log('Error:', e.message); pool.end(); });
