const { Pool } = require('pg'); const fs = require('fs'); const pool = new Pool({ connectionString: 'postgresql://postgres:soaOjumAbYOasLmvfAdwcHzguscpwfHG@shuttle.proxy.rlwy.net:12174/railway', ssl: false }); const sql = fs.readFileSync('./db/schema.sql', 'utf8'); pool.query(sql).then(() => { console.log('Done! Tables created!'); pool.end(); }).catch(e => { console.log('Error:', e.message); pool.end(); });
